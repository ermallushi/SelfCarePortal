# SelfCarePortal

ASP.NET Core MVC prototype for the ONE Business Portal.

## Run locally

```bash
dotnet restore
dotnet run
```

Then open the local URL shown by ASP.NET Core to explore the enterprise dashboard, line management controls, invoice actions, support tracking and personalized offer sections.
